{
  "name": "life-cycle",
  "version": "1.0.0",
  "description": "生活效期管理系统",
  "private": true,
  "scripts": {
    "preinstall": "echo '📦 开始安装依赖...' && npm install --prefix . --only=dev 2>/dev/null || true",
    "dev": "concurrently -n \"后端,前端\" -c \"bgBlue.bold,bgMagenta.bold\" \"npm run dev:backend\" \"npm run dev:frontend\"",
    "dev:sh": "bash start.sh",
    "dev:backend": "cd backend && node server.js",
    "dev:frontend": "cd frontend && npm run dev",
    "install:all": "npm install && cd backend && npm install && cd ../frontend && npm install"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "keywords": [
    "lifecycle",
    "habit",
    "reminder",
    "email"
  ],
  "author": "kohlarnhin",
  "license": "MIT"
}

